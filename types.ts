export interface BusinessCardData {
  id?: string;
  name: string;
  title: string;
  company: string;
  phone: string;
  email: string;
  website: string;
  summary: string;
}

export interface User {
  email: string;
}